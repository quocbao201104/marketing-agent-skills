# README workflow illustration

`from-brief-to-marketing-decision.png` was generated with the built-in ImageGen tool for the main README. Its editable specification is [the generation prompt](from-brief-to-marketing-decision.prompt.md); the illustration itself is a raster image, not an editable vector diagram.

Content is summarized from the compact runtime controller, uncertainty policy, useful-completion criteria, and final validation in `skills/marketing-agent-skills/SKILL.md` (v2.2.0). Sufficient inputs support direct execution; the optional guidance branch reads the relevant section to resolve an open question. Quality checks include evidence, reader fit, message and role fidelity, and completion of requested work. Terminal completion verifies active obligations and stops without reopening settled choices merely for optional improvement. The image describes agent instructions, not an autonomous runtime or a guarantee of results.

When the controller changes, review the direct execution path, optional guidance branch, unconnected user-checkpoint note, quality checks, and return loop before regenerating. Inspect the generated text and arrow directions before replacing the README asset.

The companion [architecture illustration](inside-the-skill.md) explains the compact core, indexed knowledge, and independent conditional supports.
